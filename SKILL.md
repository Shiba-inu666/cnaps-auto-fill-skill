---
name: cnaps-auto-fill
description: Codex 专用的开户行行号补全与核对技能。处理 Excel、CSV、TSV 等表格时保持原文件格式，只读取开户行和行号两列，只向有效业务行的行号位置写入已核验代码；一个已打开的有效来源即可填充。
---

# 开户行行号自动填充（Codex 专用）

## 数据访问边界

- 使用 Codex 的 `spreadsheets:Spreadsheets` 技能处理工作簿。
- 先按扩展名和文件特征识别真实格式，输出必须保持原扩展名、容器和数据格式；不得把 `.xls`、`.xlsm`、`.csv`、`.tsv` 或其他格式统一转换成 `.xlsx`。没有原格式无损写入能力时停止并说明。
- 每张表只扫描表头来定位“开户行”和“行号”两列。定位成功后，只读取这两列的数据单元格；不得读取、解析、导出或缓存其他列。
- 不从姓名、账号、证件号、手机号、金额、省市等其他列补充上下文，也不得把它们用于联网查询。
- 唯一允许的工作簿改动是现有行号单元格的值。不得改变任何单元格格式、数字格式、公式、行高、列宽、合并、筛选、隐藏、冻结窗格、条件格式、数据验证、打印设置、图表、图片、工作表名称、顺序或数量。
- 不得在工作簿中新增总结、审计表、标记列、批注、备注、颜色或提示文字。所有填写明细、核对结果、异常、兜底和改动说明只输出到 Codex 对话框。
- 使用能原样保留文件格式的写入方式，不得重建或转换；另存文件沿用原扩展名。CSV／TSV 还须保留原编码、BOM、分隔符、引号规则和换行符。

处理表格前阅读 [references/workbook-workflow.md](references/workbook-workflow.md)。

## 快速流程

1. 识别原文件格式，并确定同格式的无损写入方式；不能原样回写则停止。
2. 在各工作表或表格表头中识别两列。识别完成后立即停止访问其他列。
3. 只把“开户行非空且确为业务数据”的行加入目标坐标白名单。空白行、表头、说明、合计／小计、装饰行、合并单元格和公式行全部跳过。
4. 读取白名单行的行号及对应开户行；空白行号进入补全流程，已有行号进入核对流程。开户行名称去重后批量查询本地注册库：

   ```bash
   python3 scripts/cnaps_registry.py lookup --name '开户行一' --name '开户行二'
   ```

5. 仅当结果为 `auto_fill_ready` 时处理：空白单元格写入 `autofill_code`；已有值含非数字字符则记为格式错误，纯数字则与 `autofill_code` 比较。
6. 对本地未命中的开户行各检索一次。一个实际打开的来源只要明确展示同一完整开户行名称和同一 12 位行号，即可写入注册库并填充；不再等待第二来源。按 [references/source-policy.md](references/source-policy.md) 执行。
7. 没有专属支行号时，银行身份及上下级关系唯一且上级行已核验即可兜底，并标记 `parent_fallback`。
8. 每次写入前同时断言：目标坐标在白名单内、开户行非空、目标确属行号列、写入值等于已核验的 12 位 `autofill_code`。禁止写入行号、循环序号、候选序号、搜索排名或 `1/2/3/4` 等过程值。
9. 只设置目标行号值，不设置或复制样式。已有错误值默认不覆盖。另存为同格式新文件，并核对所有变化都位于白名单坐标内。

## 本地注册库

默认数据库为 `~/.codex/state/cnaps-auto-fill/registry.sqlite3`，空库会自动导入 `assets/seed-registry.jsonl`。用户可用 `CNAPS_REGISTRY_PATH` 或 `--db` 指定其他位置。

```bash
python3 scripts/cnaps_registry.py init
python3 scripts/cnaps_registry.py stats
python3 scripts/cnaps_registry.py verify \
  --name '完整规范开户行名称' --code 123456789012 \
  --source 'directory=https://已打开的来源.example/record'
```

注册库只保存开户行名称、行号、别名、来源和核验日期，不保存工作簿其他数据。

## 填写规则

- 新写入的 CNAPS 行号必须来自已核验的 12 位行号映射。
- 已有值含非数字字符时记为 `格式错误`；纯数字直接与开户行的已核验映射比较，不检查位数。
- 自动填写仅限规范名称精确匹配或明确允许自动填写的安全别名。
- 一个实际打开且名称、行号对应明确的来源即可验证并自动填充；搜索摘要或模型回答不算来源。若已经打开的来源互相冲突则不填。
- 只向目标坐标白名单写值；开户行为空或不是业务数据行时绝不写入。
- 不根据账号、卡 BIN 或相似名称推测行号，不跨银行兜底。
- 不把下级网点永久登记为上级行的安全别名，除非官方明确说明共用行号。

## 交付

不得在 Excel 内输出总结。Codex 回复中提供输出文件，并反馈所有实际写入和异常；汇总补全、`正确`、`格式错误`、`不一致`、`无法确认`和留空的数量。明细只列工作表、行号、开户行、原值、新值或核验值及原因，不总结或展示其他列。
