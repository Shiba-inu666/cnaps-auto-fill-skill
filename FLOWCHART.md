# CNAPS 自动补全与核对流程

```mermaid
flowchart TD
    A[接收原始表格文件] --> A1[识别真实格式<br/>确定同格式写入方式]
    A1 --> B[逐表扫描表头]
    B --> C{唯一识别开户行列和行号列?}
    C -- 否 --> C1[停止该工作表<br/>在 Codex 反馈异常]
    C -- 是 --> D[固定列索引<br/>之后只读取这两列]
    D --> D1{开户行非空且为业务行?}
    D1 -- 否 --> D2[跳过该行<br/>禁止任何写入]
    D1 -- 是 --> E[加入目标坐标白名单<br/>开户行名称去重]
    E --> E1[批量查询本地注册库]
    E1 --> F{取得已核验映射?}
    F -- 否 --> G[只用开户行名称联网核验]
    G --> H{一个已打开来源<br/>名称与行号明确对应?}
    H -- 否 --> H2{唯一且已核验的上级行<br/>或总行可安全兜底?}
    H2 -- 否 --> H1[空白项保持空白<br/>已有值记为无法确认]
    H2 -- 是 --> H3[采用兜底映射<br/>标记 parent_fallback]
    H -- 是 --> I[写入注册库并重新查询]
    F -- 是 --> J{行号单元格为空?}
    I --> J
    H3 --> J
    J -- 是 --> K[只写入现有行号单元格的值]
    J -- 否 --> L{含非数字字符?}
    L -- 是 --> L1[格式错误<br/>不修改原值]
    L -- 否 --> M{与已核验行号一致?}
    M -- 是 --> M1[正确]
    M -- 否 --> M2[不一致<br/>默认不修改原值]
    K --> N[只写白名单坐标<br/>保留原文件格式与结构]
    C1 --> O[Codex 对话框反馈]
    D2 --> O
    H1 --> O
    L1 --> O
    M1 --> O
    M2 --> O
    N --> O
    O --> P[提供同格式结果文件<br/>表内不写总结或标记]
```

硬性边界：保持原文件格式；识别后只读取开户行和行号两列；只向开户行非空的业务行白名单写入已核验行号；禁止把序号或过程数字写入空白行；所有明细和异常只在 Codex 对话框反馈。
